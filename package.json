{
  "name": "player1-protocol",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev",
    "compile": "npm run compile --workspace=@player1/contracts",
    "test:contracts": "npm run test --workspace=@player1/contracts",
    "coverage": "npm run coverage --workspace=@player1/contracts",
    "deploy:fuji": "npm run deploy:fuji --workspace=@player1/contracts",
    "verify:fuji": "npm run verify:fuji --workspace=@player1/contracts",
    "api:dev": "npm run dev --workspace=@player1/api",
    "api:build": "npm run build --workspace=@player1/api",
    "api:start": "npm run start --workspace=@player1/api"
  },
  "devDependencies": {
    "turbo": "2.3.3"
  },
  "packageManager": "npm@10.2.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
